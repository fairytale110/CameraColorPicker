<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tech.nicesky.library" >

    <application

        android:theme="@style/AppTheme">

        <activity android:name=".PickerActivity" android:launchMode="singleInstance"/>
    </application>
</manifest>
